version: "3.9"

services:
  # Streamlit web application
  web:
    build: .
    ports:
      - "8501:8501"
    env_file: .env
    command:
      - streamlit
      - run
      - streamlit_app.py
      - --server.port=8501
      - --server.address=0.0.0.0
    restart: unless-stopped

  # MCP HTTP server (SSE transport)
  mcp-server:
    build: .
    ports:
      - "8000:8000"
    env_file: .env
    command:
      - uvicorn
      - mcp_http_server:app
      - --host
      - "0.0.0.0"
      - --port
      - "8000"
    restart: unless-stopped
